using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestCopyCollider : MonoBehaviour
{
    // #if BLABLABLO
    // [SerializeField]
    // int yes = 0;
    // #else
    // [SerializeField]
    // int no = 1;
    // #endif
    // [SerializeField]
    // GameObject copyParent = null;
    // [SerializeField]
    // GameObject originalCollidersParent = null;
    // Start is called before the first frame update
    void Start()
    {
        // CopyColliderHelper.CreateCopyGameObjectWithColliders(originalCollidersParent, null, "_Internal");
        // foreach (var originalCollider in this.originalCollidersParent.GetComponentsInChildren<Collider>())
        // {
        //     CopyColliderHelper.CopyCollider(originalCollider, this.copyParent);
        // }
        // CopyCollider.CopyBoxCollider(originalCollider, copyParent);
    }
}
